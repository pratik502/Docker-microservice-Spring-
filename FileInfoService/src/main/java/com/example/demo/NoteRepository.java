package com.example.demo;

//@Repository
public interface NoteRepository { //extends CrudRepository<Note, Long> {
    /* 
    public List<Note> findByNote(String item);
     
   // @Query("SELECT id,note,date1 FROM Note")
    public List<Note> findAll();
    
    */
}